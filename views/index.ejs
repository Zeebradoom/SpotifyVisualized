<!DOCTYPE html>
<html>

<script>
    let waveAnimation = document.getElementById("wave-animation");
        let waveHeight = 0;

        function animateWave() {
            waveHeight = (waveHeight + 0.5) % 100; // Increase the wave height by 0.5 each time the function is called
            waveAnimation.style.height = `${waveHeight}%`; // Update the element's height
            requestAnimationFrame(animateWave); // Call the function again at the next frame
        }

        animateWave(); // Start the animation

</script>

<head>
    <title>Spotify Visualizer</title>
    <link rel="stylesheet" href="https://sp-bootstrap.global.ssl.fastly.net/8.0.0/sp-bootstrap.min.css" />
    <style>
        body {
            animation: wave 5s linear infinite;
        }

        /* Define the animation for the background */
        @keyframes wave {
        0% {
            background-color: #1db954;
        }
        25% {
            background-color: #46937e;
        }
        50% {
            background-color: #f32faa;
        }
        75% {
            background-color: #cbf656;
        }
        100% {
            background-color: #1DB954;
        }
        }

        /* Style the login button */
        .login-button {
        display: block;
        margin: auto;
        background-color: #fff;
        border-radius: 50px;
        padding: 20px;
        box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
        transition: all 0.2s ease-in-out;
        }

        .login-button:hover {
        transform: scale(1.1);
        box-shadow: 0px 8px 8px rgba(0, 0, 0, 0.25);
        }

        .login-button img {
        height: 40px;
        margin-right: 20px;
        }

        .login-button span {
        font-size: 18px;
        font-weight: 600;
        color: #1DB954;
        }

        #wave-animation {
        height: 100%;
        width: 100%;
        background: linear-gradient(180deg, #1DB954 0%, #191414 50%, #FFFFFF 100%);
        }

    </style>
</head>
<body>
    

    <h1 style="text-align: center; color: white; margin-top: 50px;">Spotify Visualizer</h1>
    <p style="text-align: center; color: white; margin-top: 20px;">See what songs and artists you've been listening to
        and share with friends!</p>
    <a href="/authorize"><button class="login-button">Login with Spotify</button></a>
    <p style="text-align: center; color: white; margin-top: 20px;">made by <a href="https://www.linkedin.com/in/derrick-cui/" 
        style="color: blue; text-decoration: underline;">Derrick</a></p>

</body>

</html>