//- extends layout.pug
    
//- block content
//-     h1 Welcome! #{user.id} ✨
//-     img(src=user.images[0].url, class='circular_image')

//-     table 
//-       each t in tracks
//-         tr 
//-           - var artist_name = t.artists[0].name
//-           - var track_name = t.name
//-           - var artist_id = t.artists[0].id
//-           - var track_id = t.id 
//-           - var album_id = t.album.id

//-           td #{artist_name}
//-           td #{track_name}
//-           //show a rectangular album cover
//-           td 
//-             img(src=t.album.images[1].url, class='rectangular_image')

//-     table 
//-       each a in artists
//-         tr 
//-           - var artist_name = a.name
//-           - var artist_id = a.id
          
//-           td #{artist_name}
//-           td 
//-             a(href='/artist/' + artist_id) View artist      